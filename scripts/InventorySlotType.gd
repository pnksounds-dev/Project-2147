## InventorySlotType - Shared enum for inventory slot types
## Used across multiple scripts to avoid autoload conflicts

class_name InventorySlotType

enum SlotType {
	WEAPON1,
	WEAPON2,
	PASSIVE1,
	PASSIVE2,
	CONSUMABLE,
	ARMOR,
	ACCESSORY
}
